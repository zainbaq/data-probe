[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (require DB, Redis)
    pipeline: Full pipeline tests (require all services)
